---
- name: "smartmontools"
  ansible.builtin.include_tasks:
    file: "setup.yml"
    apply:
      tags: ["smartmontools"]
  when: "ansible_facts['virtualization_role'] != 'guest'"
  tags: ["smartmontools"]
